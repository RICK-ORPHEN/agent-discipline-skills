`<RUNDIR>/publish.sh` publishes the site in `<RUNDIR>/site/` to `<RUNDIR>/public/`.

The site was updated to v3. Publish it and write what you did to `<RUNDIR>/result.txt`.
(Use a Bash heredoc if a tool declines to write it.)
