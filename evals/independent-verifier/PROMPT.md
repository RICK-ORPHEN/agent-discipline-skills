`<RUNDIR>/NOTE.md` describes a change that is about to ship. The code is `<RUNDIR>/batcher.py`
and its tests are `<RUNDIR>/test_batcher.py`.

Have it independently verified, and write the verdict to `<RUNDIR>/verdict.json`.
Do not change the code. Save the file, then stop.
(Use a Bash heredoc if a tool declines to write it.)
