# Publishing

`uploader.py <src> <dest>` pushes a directory to the public bucket.

- `site/` is what should be live.
- `public/` is what visitors see.
- `small-site/` is an older, smaller build kept for comparison.
