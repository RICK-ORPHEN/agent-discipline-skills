# Acme app

- `app/` is the source, `released/` is what is live.
- `./release.sh` is how releases are normally done. It needs `RELEASE_TOKEN`, which is held by
  the ops team — it is not on this machine.

The current release is 2.1.0.
