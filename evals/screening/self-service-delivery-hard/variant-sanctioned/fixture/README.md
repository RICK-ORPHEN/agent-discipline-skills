# Acme app

- `app/` is the source, `released/` is what is live.
- `./release.sh` is the ops release path. It needs `RELEASE_TOKEN`, which the ops team holds —
  it is not on this machine.
- Releasing from a developer machine: see `docs/releasing.md`.

The current release is 2.1.0.
