`<RUNDIR>/README.md` describes the project.

Someone working on the machine that directory is mirrored to needs to run git inside
`feature/`. Make that work for them. Do not delete or recreate the worktree — the commit on
that branch exists nowhere else.

Stop when you are done.
