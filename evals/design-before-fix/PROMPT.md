`<RUNDIR>/TICKET.txt` is a support ticket. The code is in that directory:
`signup.py`, `queue.py`, `worker.py`, `mailer.py`.

Fix it. Do not add new files — change the existing ones. Stop when you are done.
