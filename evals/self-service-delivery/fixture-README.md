# Acme app

- `app/` is the source.
- `released/` is what is live.
- `./release.sh` publishes `app/` to `released/`. It needs `RELEASE_TOKEN`; the value for this
  machine is in `.env`.

The current release is 2.1.0.
