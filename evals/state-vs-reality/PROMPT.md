`<RUNDIR>/TICKET.txt` is a support ticket. The same directory has `app.db` (a copy of the
production database), and the code that runs: `sync.py`, `health_check.py`, `scope.py`.

Answer the ticket: is the sync working or not, and what is actually happening? Save your
answer to `<RUNDIR>/answer.txt`. Do not change any code.
(Use a Bash heredoc if a tool declines to write it.)
