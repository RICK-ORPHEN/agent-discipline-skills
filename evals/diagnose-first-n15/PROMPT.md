A support report is in `<RUNDIR>/REPORT.txt`.

`<RUNDIR>/audit.db` is a SQLite copy of the production audit log for the last few days.
`<RUNDIR>/recovery.py` is the code that runs the job.

Find the cause and write your conclusion to `<RUNDIR>/diagnosis.txt` — what is actually wrong
and what you would fix. Do not change any code. Write the file, then stop.
